# Generated sample library. Real libraries also live here and must not be
# committed, so the whole directory is ignored rather than individual files.
/media/

# Build output
/atrium
/atrium.exe
/out/
/dist/

# Local state when running outside a container
/state/
state.json

# Go
*.test
*.out
coverage.*

# Editor and OS noise is covered by the global gitignore at
# C:\dev\.gitignore_global
